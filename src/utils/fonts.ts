import { Raleway, Roboto_Slab, Montserrat } from 'next/font/google';

export const raleway = Raleway({ subsets: ['latin'], display: 'swap' });
export const montserrat = Montserrat({ subsets: ['latin'], display: 'swap' });
export const roboto_slab = Roboto_Slab({ subsets: ['latin'], display: 'swap' });
